Place your out-of-tree or custom modules here. You can reference them in your application's *CMakeLists.txt* file with the following line:

```cmake
set(ZEPHYR_EXTRA_MODULES "${CMAKE_SOURCE_DIR}/../../modules/")
```